Завдання - пошук зображень
Створи фронтенд частину застосунку пошуку і перегляду зображень за ключовим словом. 
Додай оформлення елементів інтерфейсу. Подивись демо-відео роботи застосунку.

Форма пошуку

🔸 Форма спочатку міститья в HTML документі. Користувач буде вводити рядок для пошуку у текстове поле, а по сабміту форми необхідно виконувати HTTP-запит.
<form class="search-form" id="search-form">
  <input
    type="text"
    name="searchQuery"
    autocomplete="off"
    placeholder="Search images..."
  />
  <button type="submit">Search</button>
</form>

HTTP-запити

Для бекенду використовуй публічний API сервісу Pixabay. Зареєструйся, отримай свій унікальний ключ доступу і ознайомся з документацією.

🔸Список параметрів рядка запиту, які тобі обов'язково необхідно вказати:

    key - твій унікальний ключ доступу до API.
    q - термін для пошуку. Те, що буде вводити користувач.
    image_type - тип зображення. На потрібні тільки фотографії, тому постав значення photo.
    orientation - орієнтація фотографії. Постав значення horizontal.
    safesearch - фільтр за віком. Постав значення true.

🔸 У відповіді буде масив зображень, що задовольнили критерії параметрів запиту. Кожне зображення описується об'єктом, з якого тобі цікаві тільки наступні властивості:

    webformatURL - посилання на маленьке зображення для списку карток.
    largeImageURL - посилання на велике зображення.
    tags - рядок з описом зображення. Підійде для атрибуту alt.
    likes - кількість лайків.
    views - кількість переглядів.
    comments - кількість коментарів.
    downloads - кількість завантажень.

Якщо бекенд повертає порожній масив, значить нічого підходящого не було знайдено. У такому разі показуй повідомлення з текстом "Sorry, there are no images matching your search query. Please try again.". Для повідомлень використовуй бібліотеку notiflix.
Галерея і картка зображення

Елемент div.gallery спочатку міститься в HTML документі, і в нього необхідно рендерити розмітку карток зображень. Під час пошуку за новим ключовим словом необхідно повністю очищати вміст галереї, щоб не змішувати результати.

<div class="gallery">
  <!-- Картки зображень -->
</div>

Шаблон розмітки картки одного зображення для галереї.
<div class="photo-card">
  <img src="" alt="" loading="lazy" />
  <div class="info">
    <p class="info-item">
      <b>Likes</b>
    </p>
    <p class="info-item">
      <b>Views</b>
    </p>
    <p class="info-item">
      <b>Comments</b>
    </p>
    <p class="info-item">
      <b>Downloads</b>
    </p>
  </div>
</div>

Пагінація (2.2.2.)
🔸Pixabay API підтримує пагінацію і надає параметри page і per_page. 
🔸Зроби так, щоб в кожній відповіді приходило 40 об'єктів (за замовчуванням 20).
    Початкове значення параметра page повинно бути 1.
    🔸З кожним наступним запитом, його необхідно збільшити на 1.
   🔸 У разі пошуку за новим ключовим словом, значення page потрібно повернути до початкового, 
   оскільки буде пагінація по новій колекції зображень.

HTML документ вже містить розмітку кнопки, по кліку на яку, необхідно виконувати запит за наступною групою зображень 
і додавати розмітку до вже існуючих елементів галереї.
<button type="button" class="load-more">Load more</button>

    В початковому стані кнопка повинна бути прихована.
    Після першого запиту кнопка з'являється в інтерфейсі під галереєю.
    При повторному сабміті форми кнопка спочатку ховається, а після запиту знову відображається.

 У відповіді бекенд повертає властивість totalHits - загальна кількість зображень, які відповідають критерію пошуку (для безкоштовного акаунту). 
Якщо користувач дійшов до кінця колекції, ховай кнопку і виводь повідомлення з текстом "We're sorry, but you've reached the end of search results.".

❗Додатково
Увага
Наступний функціонал не обов'язковий для здавання завдання, але буде хорошою додатковою практикою.
Повідомлення

🔸 Після першого запиту з кожним новим пошуком отримувати повідомлення, в якому буде написано, 
скільки всього знайшли зображень (властивість totalHits). 
Текст повідомлення - "Hooray! We found totalHits images."

Бібліотека SimpleLightbox
Додати відображення великої версії зображення з бібліотекою SimpleLightbox для повноцінної галереї.

    У розмітці необхідно буде обгорнути кожну картку зображення у посилання, як зазначено в документації.
    Бібліотека містить метод refresh(), який обов'язково потрібно викликати щоразу після додавання нової групи карток зображень.

Для того щоб підключити CSS код бібліотеки в проект, необхідно додати ще один імпорт, крім того, що описаний в документації.

// Описаний в документації
import SimpleLightbox from "simplelightbox";
// Додатковий імпорт стилів
import "simplelightbox/dist/simple-lightbox.min.css";

Прокручування сторінки

Зробити плавне прокручування сторінки після запиту і відтворення кожної наступної групи зображень. Ось тобі код-підказка, але розберися у ньому самостійно.

const { height: cardHeight } = document
  .querySelector(".gallery")
  .firstElementChild.getBoundingClientRect();

window.scrollBy({
  top: cardHeight * 2,
  behavior: "smooth",
});

Нескінченний скрол
Замість кнопки «Load more», можна зробити нескінченне завантаження зображень під час прокручування сторінки. 
Ми надаємо тобі повну свободу дій в реалізації, можеш використовувати будь-які бібліотеки.


🙂ВИКОНАННЯ!

Створюємо запит на сервер.
1. Реєстрація на https://pixabay.com/api/docs/
2. В файлі img_api_serv.js створюємо функцію запиту 
2.1. Імортуємо бібліотеку axios для здійснення запитів API
- константи для використання запиту: адреса + ключ
2.2. Створюємо клас "imgApiServ" і повернемо в нього 
рядок пошуку з необхідними значенями властивостей + задамо початкове значення параметра page (повинно бути 1).
2.2.2. для запиту створюємо екземпляр класу і прописуємо необхідні параметри
- Ключ
- q - термін для пошуку. Те, що буде вводити користувач.
- image_type - тип зображення. На потрібні тільки фотографії, тому постав значення photo.
- orientation - орієнтація фотографії. Постав значення horizontal.
- safesearch - фільтр за віком. Постав значення true.
Застосовуємо пагінацію:
- per_page: 40 - робим так, щоб в кожній відповіді приходило 40 зображень
- page: 1 - Returned search results are paginated. Use this parameter to select the page number. 
2.2.3. Виконуємо запит до API за допомогою бібліотеки axios та обробляємо відповідь
- get - запит на сервер з прописаними парметрами в п. 2.2.2. (BASE_URL, { params })
- return - якщо запит вдалий з серверу повертаємо дані ({ hits, totalHits } через деструткризацію) у вигляді об'єкта
- cath - якщо помилка, ловимо її і виводимо повідомлення у консоль
2.2.4 З кожним наступним запитом, його необхідно збільшити на 1.
2.2.5.У разі пошуку за новим ключовим словом, значення page потрібно повернути до початкового, 
оскільки буде пагінація по новій колекції зображень. Використовуємо геттери і сеттери.


3. в файлі index.js 
  3.1. Додаємо код в index.html базовий код (див. HW-08 + завдання)
  3.2. Імпортуємо небхідні модулі
- simplelightbox - для створення галереї зображень (див. HW-08) + підключаємо стилі + прописуємо додаткові параметри 
- imgApiServ -  наш створений запит на сервер (див. п. 2)
- notiflix - для формування гарненьких повідомлень 🥰
  3.3. Ствоюємо посилання на необхідні об'єкти
  - '.tracker' - для спрацьовування автоскролу.
  3.4. Створюємо екземпляр класу "new imgApiServ", який буде відповідальний за "спілкування" з сервером (див п. 2.2.)
  3.5. "вішаємо" слухача на конпку (буде запускати ф-ю "onSearch")
  3.6. функція "onSearch" - отримуємо "event", в якій міститься об'єкт "currentTarget"
  - скидуємо стандартні дії браузера (preventDefault)
  - зберігаємо запит у  змінній "imgApiServ.query", 
  - якщо запит введено - очищує (оновлює) поточну сторінку ("clearGallery()" + imgApiServ.resetPage()) і  
  викликаємо ф-ю ("fetchResult()"), яка завантажує дані для галереї.
  - якщо помилка - return повідомлення про помилку інакше виведи поотчний результат

3.7. функція нескінечного скролу "infScroll.unobserve (trackEl)" - 
- створили окремий пустий div (tracker) і викликали його (в розмітці має бути останнім)
- оголосили ф-ю нескінченного скролу (стежим за div (tracker))
- оголошуємо новий екземпляр класу (new IntersectionObserver(onScroll, variable)) - https://developer.mozilla.org/ru/docs/Web/API/Intersection_Observer_API
- коли скрол наблизиться до div (tracker) ближче ніж 300рх (variable) спрацює ф-я рендеру "onScroll"
 
 3.8. функція рендеру "onScroll" - обробляє подію прокрутки на елементі "IntersectionObserver"
 - коли досягли кіця елементу еntry.isIntersecting стане true і введений запит (imgApiServ.query - не пусте) - повертаєм true
 - якщо "imgApiServ.page === 1" виконується - виходим з ф-ї оскільки вже відображається перша порція зображень.
 - інакше виконуємо функцію  настпного запиту даних на сервері (renderOnRequest())

 3.9. функція-повідомлялка  onCheckInput(totalHits) - приймає параметр кількість зображень знайдених запитом.
 - перевіряємо чи є шось у запиті та чи "totalHits" більше за два (для totalHits тому, ліміт за запитом на Pixabay складає від 3 картинок) - Determine the number of results per page.
Accepted values: 3 - 200  (https://pixabay.com/api/docs/#api_hotlinking)
- якщо дві умови true - виводимо інформацію про кількість знайдених зображень

3.10. функція renderOnRequest() - запитує на сервері наступну сторінку даних для поточного запиту. 
- передаємо сюди раніше написану функцію запиту на сервер ( ImgApiServ - див п. 2.)
- перевірям статус відповіді (then) - Якщо сторінка - перша, то функція повертає значення і не виконує додаткових запитів.
- повертаємо масив знайдених зображень і загальну кількість знайдених зображень, які доступні
- передаємо в onCheckInput результат кількості знайдених зображень
- якщо сторінка не перша то викликаємо ф-ю розмітки отриманого результату appendPicsMarkup(hits)
 
 3.11. HTML Розмітка отриманого результату appendPicsMarkup(hits)
 - витягуємо необхідні дані з масиву hits.map(({властивості}))
 - повертаємо масив робимо розмітку 
 - додаємо згенеровану розмітку в кінець елемента галереї (galleryEl.insertAdjacentHTML('beforeend', markup);)
 - функція lightbox.refresh() - повторно оновлює lightbox до нових зображень
 - слідкуємо за скролом (див. 3.7.)
 - якщо сторінка остання (перевіряємо діленням отриманих об'єктів на кількість заданих зображень) 
 то зупиняємо слідкувати за скролом.
 - виводимо повідомлення
 - інакше  викликємо функцію додавання нової сторінки.

 3.12. функція очистки галереї - cleanGallery()

 3.13. Додаємо CSS стилі
